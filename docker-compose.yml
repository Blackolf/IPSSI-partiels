# Generated by Okty.io
version: '3.6'
services:
    apache:
        image: 'httpd:2.4'
        ports:
            - '8080:8080'
        volumes:
            - './:/usr/local/apache2/htdocs'
    composer:
        image: 'composer:1.8'
        command: install
        volumes:
            - './:/app'
    php-fpm:
        build: docker/php
        volumes:
            - './:/usr/share/nginx/html'
    mysql:
        image: 'mysql:5.7'
        ports:
            - '8000:3306'
        environment:
            - MYSQL_DATABASE=Partiel
            - MYSQL_USER=Partiel-adm
            - MYSQL_PASSWORD=Partiel-admin123
            - MYSQL_RANDOM_ROOT_PASSWORD=yes
